---
- name: Exercise the safe default
  hosts: localhost
  gather_facts: true
  roles:
    - role: ansible-role-keepalived
  post_tasks:
    - name: Verify node address defaults to the inventory connection address
      ansible.builtin.assert:
        that:
          - keepalived_node_address == ansible_host
